apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.names.fullname" . }}-config-dashboard
  namespace: {{ include "common.names.namespace" . | quote }}
data:
  config_dashboard.yaml: |
{{ (.Files.Get "files/config_dashboard.yaml") | indent 4 }}
